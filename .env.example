# GEMINI_API_KEY: Required for AI API calls.
# The platform automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the UI.
GEMINI_API_KEY="AIzaSyBCvF0aik_xjLZrezalArp7NjjHK-tCfy4"


# OPENROUTER_API_KEY: Required for backup AI channel (Llama) when Gemini is unavailable.
# Configure this via the Secrets panel in the UI.
OPENROUTER_API_KEY=""

# APP_URL: The URL where this applet is hosted.
# The platform automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="https://sleuthy-sloth.github.io/92-AMXS-Tracker/"
